# DEPRECATED: This file is for local development only
# Railway uses the root Dockerfile instead
# To use locally: docker build -f Dockerfile.litellm -t jarvis-litellm .
FROM ghcr.io/berriai/litellm:main-latest

COPY litellm_config.yaml /app/litellm_config.yaml

EXPOSE 4000

CMD ["--config", "/app/litellm_config.yaml", "--port", "4000", "--host", "0.0.0.0"]
