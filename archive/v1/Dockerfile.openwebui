# DEPRECATED: This file is for local development only
# Railway uses the root Dockerfile instead
FROM ghcr.io/open-webui/open-webui:main

# Minimal configuration for cloud deployment
ENV PORT=8080
ENV HOST=0.0.0.0

EXPOSE 8080

CMD ["bash", "start.sh"]
