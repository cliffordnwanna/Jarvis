'use client'
// Phase 2: Calendar combining relationship events + Google Calendar
export default function CalendarPage() {
  return (
    <div className="p-8 text-jarvis-muted">
      <h1 className="text-2xl font-semibold text-jarvis-text mb-2">Calendar</h1>
      <p>Coming in Phase 2 — relationship events + Google Calendar sync.</p>
    </div>
  )
}
